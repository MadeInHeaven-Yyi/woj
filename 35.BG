#include<stdio.h>
#include<math.h>

int main(void) {
	int n;
	while(1) {
		scanf("%d",&n);
		if(n==0)
			break;
    int ans =(int)ceil(((double)n)/2.0);//ceil是math.h中的函数，用于对浮点数向上取整
		printf("%d\n",ans);
	}
	return 0;
}
